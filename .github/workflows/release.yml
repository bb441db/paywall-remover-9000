on:
  push:
    tags:
      - v*

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Prepare bundle
        run: |
          TAG="${{  github.ref_name }}"
          VERSION="${TAG#v}"
          echo "Version: $VERSION"
